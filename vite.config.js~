import { defineConfig } from 'vite';
import react from '@vitejs/plugin-react';

export default defineConfig({
  plugins: [react()],
  root: 'src/js', // Définir la racine pour le développement
  base: '/',      // Chemin de base pour le build
  build: {
    outDir: '../../static', // Construire les fichiers dans "src/main/resources/static"
    emptyOutDir: true,
  },
});
